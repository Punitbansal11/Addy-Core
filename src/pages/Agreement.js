import React from 'react';
import Rules from './Rules'; // Import Rules from the same directory

const Agreement = () => {
  return (
    <div className="agreement-page">
      <Rules />
    </div>
  );
};

export default Agreement;